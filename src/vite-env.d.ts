/// <reference types="vite/client" />
/**
 * Tipos do modulo `virtual:pwa-register/react`, que o vite-plugin-pwa cria em
 * tempo de build. Sem esta linha, `useAtualizacao.ts` nao compila — o import
 * aponta para um modulo que nao existe no disco.
 */
/// <reference types="vite-plugin-pwa/react" />
